query GetBorrowers {
  borrowers: user(
    where: {
      _and: [
        {user_type: {_eq: "borrower"}}
        {kyc_approved: {_eq: true}}	
      ]
    }
  ) {
    first_name
    last_name
    id
    loan_requests {
      amount
      state
      request_id
      purpose
    }
  }
}