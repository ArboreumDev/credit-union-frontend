query GetLoanRequest($requestId: uuid!) {
  loanRequest: loan_request_by_pk(request_id: $requestId) {
    request_id
    purpose
    amount
    state
    borrowerInfo {
      id
      email
      demographic_info
    }
  }
}
