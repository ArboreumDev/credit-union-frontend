query GetLoan($loanId: uuid!) {
  loan: loan_by_pk(loan_id: $loanId) {
    loan_id
    state
    principal_remaining
    repayments {
        date
        repaid_principal
        repaid_interest
    }
    lender_amounts {
      lender_id
      amount_lent
    }
  }
}
