query GetEdgesByStatus ($status: edge_status!) {
    edges: edges(where: {status: {_eq: $status} }) {
        from_user {
            id
            user_number
            name
        }
        to_user {
            id
            user_number
            name
        }
        trust_amount
    }
}